task hello << {
    println "hello"

    println "using build file '$buildFile.name' in '$buildFile.parentFile.name'."
}

task world {
    println "world"

    println "using build file '$buildFile.name' in '$buildFile.parentFile.name'."
}
